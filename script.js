// Shineway website: product/order buttons already open WhatsApp.
// To add real products later, replace the sample product cards in index.html
// with your own bag photos, names and prices.
document.querySelectorAll('a[href^="#"]').forEach(link=>{
  link.addEventListener('click',()=>{});
});
